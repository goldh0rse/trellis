# Compiled binary (go build ./cmd/trellis)
/trellis

# Persisted chain databases (the CLI default plus any per-node files)
*.db

# Wallet seeds — UNENCRYPTED secrets, never commit
/wallets.dat

# Test binaries and profiling / coverage output
*.test
*.out
*.prof
coverage.*

# Go workspace files
go.work
go.work.sum

# OS / editor cruft
.DS_Store

# Local planning docs (kept out of the repository)
docs/plans/
